// PinShop default configuration.
//
// These are the defaults baked into the site for everyone who loads it.
// A visitor can override them via the ⚙ settings panel (stored in their
// browser's localStorage). Edit and commit this file to change the defaults.

window.PINSHOP_CONFIG = {
  // Your deployed Cloudflare Worker URL, e.g.
  // "https://pinterest-shop-worker.YOUR-SUBDOMAIN.workers.dev"
  workerUrl: "",

  // Your Amazon Associates tag, e.g. "yourtag-20".
  // Leave blank to produce plain (non-affiliate) Amazon search links.
  amazonTag: "",
};
